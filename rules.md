# rules.md

Decision log. Consult only as a fallback — after `spec.md`, the active `ep-*.md`, and `CLAUDE.md` fail to answer a question. If this file doesn't answer it either, ask the dev; never guess.

A rule is added here only when the dev corrects a proposed approach and explicitly asks to save it. Do not add speculative or inferred rules.
